interface:
  display_name: "Small Loop Skill (SLK)"
  short_description: "One Supervisor/Checker with one Worker"
  default_prompt: "Use only $small-loop-skill (SLK), exactly once. Never load, borrow, combine, or switch to MSLK. Use exactly one visible combined Supervisor/Checker and one visible Worker, never subagents or a separate Checker. Supervisor responsibility owns planning, GO revision, and Worker-result repair; Checker responsibility inside the same combined role owns validation and routing. Use gpt-5.6-sol xhigh for the combined role and gpt-5.5 high through gpt-5.6-sol high for the Worker by task type. Size GO by project need and CELL by computer capacity. Show accepted/total project CELL progress on every task until total/total. Simulate before formal work and every GO revision."
