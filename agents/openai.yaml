interface:
  display_name: "Small Loop Skill (SLK)"
  short_description: "One Supervisor/Checker with one Worker"
  default_prompt: "Use $small-loop-skill (SLK) with the current thread combining Supervisor and Checker, launch one fresh Worker for a new project, and upgrade to MSLK only when multiple necessary Workers are acceptance-independent and immediately startable."
