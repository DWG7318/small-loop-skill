interface:
  display_name: "Small Loop Skill (SLK)"
  short_description: "One Supervisor/Checker with one Worker"
  default_prompt: "Use $small-loop-skill (SLK) with a 5.6 Sol xhigh Supervisor/Checker, launch one fresh 5.6 Terra or Sol Worker at medium or higher, assign its model per CELL, and upgrade to MSLK only when multiple necessary Workers are acceptance-independent and immediately startable."
