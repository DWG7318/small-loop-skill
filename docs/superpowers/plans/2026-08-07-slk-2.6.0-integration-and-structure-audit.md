# SLK 2.6.0 Integration and Structure Audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the already completed SLK 2.6.0 model policy onto the latest released main, prove that SLK remains structurally lean, and prepare one verified mainline candidate.

**Architecture:** Keep the repository's source/install mirror because the publication contract requires byte-identical install assets. Do not split the bounded serial method into extra runtime modules unless a measured duplicate authority or behavior exists. Integrate the model-policy branch, resolve only evidence-backed conflicts, then validate source, install mirror, manifest, optimized execution, and the full suite.

**Tech Stack:** Markdown contracts, JSON Schema, YAML templates, Python validators, pytest, Git.

---

### Task 1: Integrate the completed model-policy history

**Files:**
- Merge: `feature/slk-2.6.0-model-policy`
- Preserve: `scripts/validate_defect_repair.py`
- Preserve mirror: `small-loop-skill/`

- [ ] **Step 1: Confirm the latest released base**

Run: `git rev-parse HEAD && git describe --tags --exact-match HEAD`

Expected: the worktree starts at the current remote `main` release.

- [ ] **Step 2: Merge the completed feature history**

Run: `git merge --no-ff feature/slk-2.6.0-model-policy`

Expected: the 2.6.0 policy is integrated without dropping the 2.5.1 causal-experiment fix.

- [ ] **Step 3: Run focused compatibility checks**

Run: `python -m pytest -q tests/test_causal_defect_repair.py tests/test_runtime_control.py`

Expected: all focused tests pass.

### Task 2: Prove that no structural slimming is justified

**Files:**
- Inspect: `scripts/validate_runtime_control.py`
- Inspect: `tests/test_runtime_control.py`
- Inspect: `scripts/validate_repository.py`
- Inspect: `small-loop-skill/`

- [ ] **Step 1: Measure canonical source and test surfaces**

Run: count physical lines separately for root production scripts, tests, and the install mirror.

Expected: install-tree duplication is classified as required distribution output, not duplicate authority.

- [ ] **Step 2: Check authority ownership and mirror identity**

Run: `python scripts/validate_repository.py`

Expected: source/install files are byte-identical and all canonical responsibilities have one implementation owner.

- [ ] **Step 3: Reject unnecessary restructuring**

Record the decision in `.codex/decision-log.md`: SLK stays unsplit unless a later measured defect demonstrates duplicate authority or unreachable behavior.

### Task 3: Refresh release evidence and verify the candidate

**Files:**
- Modify: `MANIFEST.json`
- Modify: `VALIDATION-REPORT.md`
- Modify: `.codex/decision-log.md`
- Modify: `.codex/command-notes.md`

- [ ] **Step 1: Refresh the deterministic manifest**

Run: `python scripts/validate_repository.py --write-manifest`

Expected: `MANIFEST.json` contains every release file and current SHA-256 values.

- [ ] **Step 2: Run optimized and full validation**

Run: `python -O -m pytest -q tests/test_runtime_control.py`

Run: `python -m pytest -q`

Expected: both commands pass with zero failures.

- [ ] **Step 3: Validate the repository and Git diff**

Run: `python scripts/validate_repository.py`

Run: `git diff --check`

Expected: both commands pass and the worktree contains only intended SLK 2.6.0 integration/audit changes.

- [ ] **Step 4: Commit the integration candidate**

Run: stage only the confirmed paths, then `git commit -m "chore: integrate and audit SLK 2.6.0"`.

Expected: one clean, reviewable candidate on the integration branch.
