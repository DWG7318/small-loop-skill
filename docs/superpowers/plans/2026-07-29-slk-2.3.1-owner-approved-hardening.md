# SLK 2.3.1 Owner-Approved Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish SLK 2.3.1 as Small Loop Skill with exactly one visible Control conversation, one persistent Worker conversation, three isolated Control responsibility modes, fail-closed receipts, and enforceable serial Run contracts.

**Architecture:** Keep the repository root and `small-loop-skill/` install tree synchronized. Represent the method contract in JSON, human guidance in `SPEC.md` and `SKILL.md`, and executable repository/plan validation in Python. All success states are explicit outputs, never template defaults.

**Tech Stack:** Markdown, YAML, JSON, Python 3.12+, pytest, PyYAML, GitHub Actions.

---

### Task 1: Freeze Owner decisions and release identity

**Files:**
- Modify: `VERSION`
- Modify: `README.md`
- Modify: `README.zh-CN.md`
- Modify: `CHANGELOG.md`
- Modify: `MIGRATION.md`
- Modify: `agents/openai.yaml`
- Test: `tests/test_contract.py`

- [ ] **Step 1: Write failing identity and topology tests**

Assert version `2.3.1`, canonical name `Small Loop Skill`, exactly `CONTROL` and `WORKER`, one Worker, one Active CELL, and Control modes `SUPERVISOR`, `CHECKER`, `VERIFIER`.

- [ ] **Step 2: Run the focused tests and verify RED**

Run: `python -m pytest -q tests/test_contract.py`

Expected: failures against the 1.9.1 identity and two-mode contract.

- [ ] **Step 3: Update release identity and migration documentation**

Document that `Serial Loop Kit` is rejected as canonical identity, Calabash is supplied through a frozen LCCoding Run Contract, and the two-visible-conversation topology is retained.

- [ ] **Step 4: Run the focused tests and verify GREEN**

Run: `python -m pytest -q tests/test_contract.py`

Expected: identity and topology assertions pass.

### Task 2: Make receipts and state transitions fail closed

**Files:**
- Modify: `contracts/slk-control-kernel.json`
- Modify: `small-loop-skill/contracts/slk-control-kernel.json`
- Create: `templates/*.yaml`
- Create: `small-loop-skill/templates/*.yaml`
- Test: `tests/test_contract.py`

- [ ] **Step 1: Write failing receipt-default and authority tests**

Assert all D0-D3 and Owner receipt templates default to `PENDING`; Worker owns D0, Checker owns D1, Verifier owns D2/D3, and only Owner signs `LOOP_OWNER_ACCEPTED`.

- [ ] **Step 2: Verify RED**

Run: `python -m pytest -q tests/test_contract.py -k "receipt or topology"`

Expected: missing templates and responsibility mappings fail.

- [ ] **Step 3: Implement the minimum Receipt Envelope and state model**

Include stable receipt, contract, baseline, candidate, responsibility, environment, evidence, consumed-receipt, invalidation, and supersession fields. Keep Current/Active as Run pointers separate from GO lifecycle state.

- [ ] **Step 4: Verify GREEN**

Run: `python -m pytest -q tests/test_contract.py -k "receipt or topology"`

Expected: all focused tests pass.

### Task 3: Enforce strict Serial Plan behavior

**Files:**
- Create: `scripts/validate_serial_plan.py`
- Create: `small-loop-skill/scripts/validate_serial_plan.py`
- Create: `templates/serial-plan.yaml`
- Create: `small-loop-skill/templates/serial-plan.yaml`
- Create: `examples/minimal-run/serial-plan.yaml`
- Test: `tests/test_serial_plan_validator.py`

- [ ] **Step 1: Write failing negative tests**

Cover empty plans, missing GO contracts, duplicate IDs, invalid ordinals, multiple Current/Active pointers, successor release without predecessor D2 PASS, and `python -O` execution.

- [ ] **Step 2: Verify RED**

Run: `python -m pytest -q tests/test_serial_plan_validator.py`

Expected: validator module is absent or accepts invalid fixtures.

- [ ] **Step 3: Implement explicit validation errors**

Return stable `SLK_PLAN_*` error codes through explicit exceptions and exit status 1; do not use `assert` for input validation.

- [ ] **Step 4: Verify GREEN in normal and optimized Python**

Run: `python -m pytest -q tests/test_serial_plan_validator.py`

Expected: valid plan passes and every invalid plan fails in both modes.

### Task 4: Align normative specification and safety boundaries

**Files:**
- Create: `SPEC.md`
- Modify: `SKILL.md`
- Modify: `small-loop-skill/SKILL.md`
- Create/Modify: `references/*.md`
- Create/Modify: `small-loop-skill/references/*.md`
- Test: `tests/test_contract.py`

- [ ] **Step 1: Write failing normative marker tests**

Assert required D2 PASS, Baseline Amendment-only resolution, security hard brakes, candidate invalidation, Run Owner Acceptance, and `LOOP_OWNER_ACCEPTED` output boundary.

- [ ] **Step 2: Verify RED**

Run: `python -m pytest -q tests/test_contract.py`

Expected: old Calabash-owned and Checker-GO-acceptance text fails.

- [ ] **Step 3: Replace current operational guidance with the approved 2.3.1 contract**

Keep two visible conversations and one active Control mode at a time; define Verifier independence as responsibility and clean-environment isolation without claiming blind conversation-memory isolation.

- [ ] **Step 4: Verify GREEN**

Run: `python -m pytest -q tests/test_contract.py`

Expected: normative contract tests pass.

### Task 5: Enforce repository integrity and cross-platform behavior

**Files:**
- Create/Modify: `scripts/validate_repository.py`
- Create/Modify: `MANIFEST.json`
- Create/Modify: `VALIDATION-REPORT.md`
- Modify: `.github/workflows/validate.yml`
- Modify: `LICENSE`
- Test: `tests/test_repository_validator.py`

- [ ] **Step 1: Write failing integrity tests**

Cover version drift, unlisted files, missing files, hash mismatch, incomplete MIT text, UTF-8 reads, and root/install-tree drift.

- [ ] **Step 2: Verify RED**

Run: `python -m pytest -q tests/test_repository_validator.py`

Expected: current repository lacks manifest enforcement and complete license text.

- [ ] **Step 3: Implement integrity validation and Windows CI**

Define Manifest exclusions, validate the full release file set and hashes, use explicit UTF-8, and run the same suite on Ubuntu and Windows.

- [ ] **Step 4: Verify GREEN**

Run: `python scripts/validate_repository.py && python -m pytest -q`

Expected: repository validation passes and the full test suite is green.

### Task 6: Publish through reviewed GitHub history

**Files:**
- Update: `MANIFEST.json`
- Update: `VALIDATION-REPORT.md`

- [ ] **Step 1: Run final verification**

Run: `python scripts/validate_repository.py`, `python scripts/validate_serial_plan.py examples/minimal-run/serial-plan.yaml`, `python -m pytest -q`, and a temporary install-tree smoke check.

- [ ] **Step 2: Inspect scope and commit intentionally**

Run: `git status -sb`, `git diff --stat`, and `git diff --check` before staging only SLK 2.3.1 files.

- [ ] **Step 3: Push and open a ready pull request to main**

Use branch `agent/slk-2.3.1-hardening`, then push and create a PR describing the Owner-approved identity/topology and validation evidence.

- [ ] **Step 4: Merge after GitHub checks pass**

Merge into `main`, verify remote HEAD, create annotated tag `v2.3.1`, push the tag, and confirm repository/version/tag alignment.

## Self-review

- Spec coverage: all twelve author-reviewed findings and both Owner decisions map to Tasks 1-5.
- Placeholder scan: no implementation placeholder is used; each behavior has an explicit test or artifact.
- Type consistency: responsibility names, receipt layers, pointers, and version `2.3.1` remain consistent across tasks.
