# LE BI Integration Plan

- [ ] Lock the SLK-only row, source-context, expansion, archive, and native-window contracts with failing tests.
- [ ] Correct lifecycle semantics: allow multiple open SLKs per project and archive only completed, abandoned, or explicitly superseded Runs.
- [ ] Add concise Run/source metadata while preserving existing state databases and legacy GO storage compatibility.
- [ ] Adapt the downloaded Figma visual language to the existing React/Tauri app without demo data, Tailwind, remote fonts, or fake browser controls.
- [ ] Remove GO from active SLK method language; align CLK as SLK composition and GLK as a Node DAG whose Nodes are SLKs.
- [ ] Run focused UI, Rust, Python, build, and visual verification; then synchronize validated global skill copies.
