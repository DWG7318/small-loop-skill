{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal read-only capability for the SLK BI main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
